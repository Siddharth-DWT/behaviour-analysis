# NEXUS Voice Agent - Dependencies
# NOTE: Heavy packages (torch, pyannote, speechbrain, matplotlib, filterpy, parselmouth)
# are installed in separate Dockerfile layers for caching. Only lightweight deps here.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Audio processing
librosa==0.10.2
soundfile==0.13.1
numpy==1.26.4
numba>=0.59.0
av>=12.0.0
scikit-learn>=1.3.0

# Whisper transcription (local via faster-whisper for speed)
faster-whisper==1.1.0

# HuggingFace hub (pyannote.audio already installed in Dockerfile)
huggingface_hub>=0.20.0

# Database
psycopg2-binary==2.9.10
sqlalchemy==2.0.36
asyncpg==0.30.0

# Redis
redis[hiredis]==5.2.1

# HTTP client (for external GPU Whisper API)
httpx>=0.25.0

# Utilities
pydantic==2.10.4
python-dotenv==1.0.1
