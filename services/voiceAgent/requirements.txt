# NEXUS Voice Agent - Dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Audio processing
librosa==0.10.2
soundfile==0.13.1
numpy==1.26.4
av>=12.0.0
scikit-learn>=1.3.0

# Whisper transcription (local via faster-whisper for speed)
faster-whisper==1.1.0

# Speaker diarization
pyannote.audio==3.4.0
huggingface_hub>=0.20.0,<0.24.0

# Speaker embeddings (GE2E encoder for speaker ID clustering)
resemblyzer>=0.1.3
# torch + torchaudio installed separately in Dockerfile (CPU-only build)

# Database
psycopg2-binary==2.9.10
sqlalchemy==2.0.36
asyncpg==0.30.0

# Redis
redis[hiredis]==5.2.1

# HTTP client (for external GPU Whisper API)
httpx>=0.25.0

# Utilities
pydantic==2.10.4
python-dotenv==1.0.1
